import assert from 'node:assert/strict'; import { Entities } from '../core/entity-store.js'; Entities.hydrateFromState({correspondence:[{id:'1',referenceId:'REF-C',department:'DGO',status:'closure_check'}],tracking:[{id:'T1',referenceId:'REF-C',department:'DGO',status:'in_progress'}]}); assert.equal(Entities.canClose('REF-C').ok,false); console.log('closure-gate-contract passed');
