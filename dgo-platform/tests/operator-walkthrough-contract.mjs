import assert from 'node:assert/strict';import { Routes } from '../config/routes.config.js';const paths=Routes.map(r=>r.path);for(const p of ['correspondence','single-assignment','bulk-assignment','orchestrator','response-tracking','approvals','dispatch','archive','lookup','diagnostics'])assert.ok(paths.includes(p),p+' route missing');console.log('operator-walkthrough-contract passed');
